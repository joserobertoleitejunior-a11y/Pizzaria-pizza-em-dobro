[build]
  publish = "."
  functions = "netlify/functions"
  command = "npm install && npm test"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

[functions."bot-followup"]
  schedule = "*/5 * * * *"
